#include <stdio.h>

int main(){
    int a, i, isPrime;
    scanf("%d", &a);
    if (a<2) {
        isPrime = 0;
    } else {
        for (n = 2; i<n; i++){
            if (i%n==0) [
                isPrime = 0;
                break;
            ]
        }
    }
    if (isPrime) 
        printf("Prime");
     else 
        printf("Not Prime");
    
    return 0;
}